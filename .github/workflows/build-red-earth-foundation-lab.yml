name: Build Red Earth Foundation Lab

on:
  workflow_dispatch:
  push:
    branches: ["main"]
    paths:
      - "cart_source/red_earth_foundation_lab/**"
      - "carts/red_earth_foundation_lab.index.json"
      - ".github/workflows/build-red-earth-foundation-lab.yml"

permissions:
  contents: write
  actions: write

concurrency:
  group: red-earth-foundation-lab-build
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Audit clean foundation
        run: |
          python - <<'PY'
          import json
          from pathlib import Path

          cart = json.loads(Path("cart_source/red_earth_foundation_lab/cart.json").read_text())
          assert cart["id"] == "red_earth_foundation_lab"
          assert cart["version"] == "0.1.0"
          assert cart["base"] == "red"
          assert cart["seal"] == "open"
          assert cart.get("speeds") == [1]

          expected = [
              "gen1_kaizo",
              "potato_voxel",
              "overworld_wild_spawns",
              "performance_monitor",
          ]
          ids = [m["id"] for m in cart["mods"]]
          assert ids == expected, (ids, expected)
          assert cart["load_order"] == expected
          assert len(ids) == len(set(ids))

          by_id = {m["id"]: m for m in cart["mods"]}
          assert by_id["gen1_kaizo"]["version"] == "0.8.3"
          assert by_id["gen1_kaizo"]["options"] == {"battle": "static"}

          potato = by_id["potato_voxel"]
          assert potato["version"] == "1.9.6"
          assert "options" not in potato

          wilds = by_id["overworld_wild_spawns"]
          assert wilds["version"] == "2.1.9"
          assert "options" not in wilds

          assert by_id["performance_monitor"]["version"] == "1.4.1"

          forbidden = {
              "irregular_origin", "philosopher_stones",
              "red_earth_kaizo_bridge", "red_earth_irregular_upgrade",
              "red_earth_shiny_bridge", "red_earth_regional_shiny_art",
              "red_earth_shiny_fx_bridge", "red_earth_dramaless_battle_bridge",
              "red_earth_voxel_profile_next",
              "BATTLE_ART_VOXEL_FORK", "DRAMATIC_SHAPE", "DRAMALESS_SHAPE",
              "FOLLOWERS_EX", "PokePCFollowers_VoxelMerge",
              "crystal_animated_sprites_with_shiny_visuals",
          }
          assert forbidden.isdisjoint(ids), sorted(forbidden.intersection(ids))

          print("FOUNDATION LAB AUDIT PASS:", " -> ".join(ids))
          PY

      - name: Fetch official cartkit
        run: |
          curl -fsSL \
            https://raw.githubusercontent.com/bryanthaboi/gen1recomp/dev/tools/cartkit.py \
            -o /tmp/cartkit.py

      - name: Pack clean lab cart
        run: |
          python /tmp/cartkit.py pack cart_source/red_earth_foundation_lab \
            -o /tmp/red_earth_foundation_lab-0.1.0.g1rcart

      - name: Publish clean lab cart
        run: |
          mkdir -p "site/data/carts/DuskPromised@red_earth_foundation_lab"
          cp /tmp/red_earth_foundation_lab-0.1.0.g1rcart \
            "site/data/carts/DuskPromised@red_earth_foundation_lab/red_earth_foundation_lab-0.1.0.g1rcart"
          cp "site/data/carts/DuskPromised@red_earth_next/thumbnail.jpg" \
            "site/data/carts/DuskPromised@red_earth_foundation_lab/thumbnail.jpg"

          python - <<'PY'
          import hashlib, json
          from pathlib import Path

          cart_path = Path("site/data/carts/DuskPromised@red_earth_foundation_lab/red_earth_foundation_lab-0.1.0.g1rcart")
          meta_path = Path("carts/red_earth_foundation_lab.index.json")
          source_path = Path("cart_source/red_earth_foundation_lab/cart.json")

          meta = json.loads(meta_path.read_text(encoding="utf-8"))
          source = json.loads(source_path.read_text(encoding="utf-8"))
          body = cart_path.read_bytes()

          for key in ("title","author","summary","version","base","seal","speeds",
                      "shell","finish","mods","load_order"):
              if key in source:
                  meta[key] = source[key]

          meta["latest"] = {
              "version": source["version"],
              "name": source["version"],
              "prerelease": True,
              "zip": {
                  "name": cart_path.name,
                  "url": "https://duskpromised.github.io/gen1recomp-mod-index/data/carts/DuskPromised@red_earth_foundation_lab/" + cart_path.name,
                  "size": len(body),
                  "sha256": hashlib.sha256(body).hexdigest()
              }
          }
          meta["thumbnail"] = "data/carts/DuskPromised@red_earth_foundation_lab/thumbnail.jpg"
          meta["update_check"] = "ok"
          meta_path.write_text(json.dumps(meta, indent=2, ensure_ascii=False) + "\n", encoding="utf-8")

          print("cart bytes:", len(body))
          print("cart sha256:", hashlib.sha256(body).hexdigest())
          PY

      - name: Commit publication
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            "site/data/carts/DuskPromised@red_earth_foundation_lab/thumbnail.jpg" \
            "site/data/carts/DuskPromised@red_earth_foundation_lab/red_earth_foundation_lab-0.1.0.g1rcart" \
            "carts/red_earth_foundation_lab.index.json"
          if git diff --cached --quiet; then
            echo "Foundation Lab cart is already published."
            exit 0
          fi
          git commit -m "Publish Red Earth Foundation Lab v0.1.0"
          git pull --rebase origin main
          git push

      - name: Refresh combined Gen 1 index
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh workflow run sync.yml --ref main
